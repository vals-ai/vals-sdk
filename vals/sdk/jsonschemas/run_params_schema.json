{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Run Configuration Parameters",
  "properties": {
    "maximum_threads": {
      "type": "integer",
      "description": "The maximum number of threads to run in parallel",
      "minimum": 1,
      "maximum": 64
    },
    "description": {
      "type": "string",
      "description": "An additional description to add to the run"
    },
    "use_fixed_output": {
      "type": "boolean",
      "description": "If we should use the output given or query the API"
    }
  }
}
